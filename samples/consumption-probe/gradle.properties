org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options\="-Xmx2048M"
# Same reason as the main build: no host can build every declared target, and a host that cannot
# build one should still be able to run the probe for the targets it can.
kotlin.native.ignoreDisabledTargets=true
